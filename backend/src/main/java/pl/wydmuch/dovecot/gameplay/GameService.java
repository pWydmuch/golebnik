package pl.wydmuch.dovecot.gameplay;

import pl.wydmuch.dovecot.games.tictactoe.engine.TicTacToeMove;

public class GameService {

    public void makeMove(TicTacToeMove ticTacToeMove, String roomId, String sessionId){
//        Field.FieldContent playerSign = ((TicTacToePlayer) rooms.get(roomId).getCurrentPlayer()).getPlayerSign();
//        ticTacToeMove.setPlayerSign(playerSign);
//        rooms.get(roomId).makeMove(ticTacToeMove);
    }



}
