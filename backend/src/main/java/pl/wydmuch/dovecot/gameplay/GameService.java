package pl.wydmuch.dovecot.gameplay;

import pl.wydmuch.dovecot.games.tictactoe.Field;
import pl.wydmuch.dovecot.games.tictactoe.Move;
import pl.wydmuch.dovecot.room.TicTacToePlayer;

public class GameService {

    public void makeMove(Move move, String roomId, String sessionId){
//        Field.FieldContent playerSign = ((TicTacToePlayer) rooms.get(roomId).getCurrentPlayer()).getPlayerSign();
//        move.setPlayerSign(playerSign);
//        rooms.get(roomId).makeMove(move);
    }



}
