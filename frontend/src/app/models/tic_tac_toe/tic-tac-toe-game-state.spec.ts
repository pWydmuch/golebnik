import { TicTacToeGameState } from './tic-tac-toe-game-state';

describe('TicTacToeGameState', () => {
  it('should create an instance', () => {
    expect(new TicTacToeGameState()).toBeTruthy();
  });
});
